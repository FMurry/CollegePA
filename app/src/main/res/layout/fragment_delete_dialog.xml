<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="10dp">

    <TextView
        android:id="@+id/delete_dialog_question"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Are you sure?"
        android:layout_centerHorizontal="true"
        android:layout_margin="16dp"/>

    <TextView
        android:id="@+id/delete_dialog_yes"
        android:layout_width="wrap_content"
        android:layout_below="@id/delete_dialog_question"
        android:layout_height="wrap_content"
        android:textColor="@color/bluegray"
        android:layout_toStartOf="@id/delete_dialog_question"
        android:text="Yes"/>

    <TextView
        android:id="@+id/delete_dialog_no"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/delete_dialog_question"
        android:layout_toEndOf="@id/delete_dialog_question"
        android:textColor="@color/red"
        android:text="No"/>

    <TextView
        android:id="@+id/delete_dialog_warning"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/delete_dialog_yes"
        android:text="This can't be undone"
        android:layout_centerHorizontal="true"
        android:textColor="@color/red"
        android:layout_margin="16dp"/>

</RelativeLayout>